var expect = require('chai').expect;
var utils = require('./utils');
var bikeasoc = require('../app');

describe('init',function(){
  it('works',function(done){
    expect(true).to.be.true;
    done();
  });
});
